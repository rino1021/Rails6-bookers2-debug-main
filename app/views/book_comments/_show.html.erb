            <tr>
            <%= form_with model:@book do |f| %>
              <%= f.text_area :book_comment, rows: '5', placeholder: "コメントをここに" %>
              <%= f.submit "送信する" %>
            <% end %>
            </tr>